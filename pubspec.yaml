name: untitled9
description: "A new Flutter project."
publish_to: 'none' # Change to remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  image_picker: ^1.1.2
  intl: ^0.19.0
  google_fonts: ^6.2.1
  permission_handler: ^11.3.1
  flutter_localizations:
    sdk: flutter
  animated_bottom_navigation_bar: ^1.3.3
  navigation_view: ^1.0.6
  dot_curved_bottom_nav: ^0.0.2
  iconly: ^1.0.1
  cupertino_icons: ^1.0.8
  firebase_storage: ^12.3.3
  http: ^1.2.2
  curved_navigation_bar: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.0.11
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/